"""How a case has behaved across runs: its pooled pass-rate and how each score
has moved. Read over the mainline, for the dashboard to draw. Nothing here
gates a test, and nothing in `core` depends on it.
"""
